# Coding Challenges

Challenges from [CC](https://codingchallenges.fyi)

> To generate the binaries:

`pyinstaller` is installed globally, so use uvx like so (inside the specific challenge folder):

`uvx pyinstaller -n p-<EXECUTABLE_NAME> --onefile main.py`